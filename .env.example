# Puller backend (offline game downloads). Keep PULLER_PORT and PUBLIC_DOWNLOADER_URL in sync.
PULLER_PORT=18787
PUBLIC_DOWNLOADER_URL=http://127.0.0.1:18787

# Optional: force offline backend (public-site = IndexedDB, local-app = puller on disk)
# PUBLIC_OFFLINE_DEPLOYMENT=local-app
